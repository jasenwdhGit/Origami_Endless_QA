# 刷题程序 - 规格说明书

## 1. 项目概述

- **项目名称**: QuizMaster - 智能刷题系统
- **项目类型**: 单页Web应用（纯前端）
- **核心功能**: 支持Excel导入、顺序刷题、随机刷题、错题复习的交互式刷题工具
- **目标用户**: 需要大量做题练习的学生、考试备考者

## 2. 视觉与界面设计

### 整体风格
- **设计语言**: 现代卡片式设计，灵感来自Notion和Linear
- **色彩方案**:
  - 主色: `#6366F1` (Indigo 500)
  - 次色: `#8B5CF6` (Violet 500)
  - 强调色: `#10B981` (Emerald 500) 用于正确 / `#EF4444` (Red 500) 用于错误
  - 背景: `#F8FAFC` (浅灰) / `#FFFFFF` (卡片)
  - 文字: `#1E293B` (深灰) / `#64748B` (次要文字)
- **字体**: "Source Han Sans SC" (思源黑体), "Noto Sans SC", sans-serif
- **动效**: 卡片悬停微提升、选项点击涟漪效果、答案揭晓滑入动画、进度条流畅过渡

### 布局结构
```
┌─────────────────────────────────────────────┐
│  Header: Logo + 模式切换标签 + 统计信息      │
├─────────────────────────────────────────────┤
│                                             │
│  主内容区 (根据状态显示不同内容):             │
│  1. 初始状态: 上传区域 + 历史记录            │
│  2. 刷题状态: 题目卡片 + 选项 + 操作按钮     │
│  3. 结果状态: 答题统计 + 错题列表             │
│                                             │
├─────────────────────────────────────────────┤
│  Footer: 进度条 + 导航按钮                   │
└─────────────────────────────────────────────┘
```

### 组件设计

#### 上传区域
- 虚线边框卡片，居中显示
- 拖拽文件时边框变为主色，背景轻微变色
- 支持点击或拖拽上传Excel文件

#### 题目卡片
- 白色卡片，圆角12px，轻微阴影
- 顶部显示题号、题型标签
- 题目文字使用较大字号(18px)
- 选项列表垂直排列，每项带圆形单选/多选框

#### 选项组件
- 未选中: 白色背景，灰色边框
- 悬停: 浅色背景，边框变主色
- 选中: 主色背景，白色文字
- 正确: 绿色背景 + 对勾图标
- 错误: 红色背景 + 叉号图标

#### 进度条
- 固定在底部
- 显示当前进度 (如 "5/50")
- 流畅的宽度动画

## 3. 功能与交互设计

### 核心功能

#### 1. Excel文件导入
- 支持 .xlsx, .xls 格式
- Excel格式要求:
  - 第一行: 表头 (正确答案, 题目内容, 选项A, 选项B, 选项C, 选项D)
  - 每行一条题目
- 解析后显示总题数
- 自动保存到浏览器localStorage

#### 2. 刷题模式
- **顺序刷题**: 按Excel中的顺序依次出题
- **随机刷题**: 随机打乱题目顺序
- **错题复习**: 只显示之前答错的题目

#### 3. 答题交互
- 单选题: 点击选项即选中，再次点击可换选
- 判断题: 两个选项 (正确/错误)
- 点击"确认答案"显示结果
- 正确: 绿色高亮 + "正确!" 提示 + 自动进入下一题(1.5秒后)
- 错误: 红色高亮正确答案 + 显示解析 + "下一题"按钮
- **左右切换按钮**: 可在答题过程中切换上一题/下一题
- **滑屏切换**: 移动端支持左右滑动手势切换题目
- **进度条选题**: 点击进度条任意位置可快速跳转到指定题目

#### 4. 错题记录
- 答错的题目自动记录到错题本
- 错题记录包含: 题目、用户答案、正确答案
- 错题可单独复习或重置

#### 5. 统计功能
- 实时显示: 正确数、错误数、正确率
- 完成一轮后显示总结报告
- 本地保存历史记录

### 交互细节

| 动作 | 响应 |
|-----|-----|
| 拖拽Excel文件到上传区 | 边框变色，显示"松开以上传" |
| 释放文件 | 解析中显示loading动画，成功后跳转刷题 |
| 点击选项 | 选项高亮，涟漪动画 |
| 点击确认答案 | 禁用选项点击，显示结果动画 |
| 答对 | 选项变绿 + 绿色闪光 + 1.5秒后下一题 |
| 答错 | 选项变红 + 显示正确答案(绿色) |
| 点击下一题 | 卡片滑出，新卡片滑入 |
| 点击退出刷题 | 弹出确认框，确认后返回首页 |

### 空状态与边界情况
- 无题目: 显示"请先上传题目文件"
- Excel格式错误: 显示"文件格式不正确，请检查Excel结构"
- 所有题目完成: 显示完成总结卡片
- 错题本为空: 显示"太棒了！暂无错题"

## 4. 技术方案

### 技术栈
- **HTML5 + CSS3 + Vanilla JavaScript** (无框架依赖)
- **SheetJS (xlsx.js)** - Excel文件解析
- **LocalStorage** - 数据持久化

### 文件结构
```
/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式文件
├── js/
│   ├── app.js          # 主应用逻辑
│   ├── excel.js        # Excel解析模块
│   └── storage.js      # 本地存储模块
└── README.md           # 使用说明
```

### 数据结构

```javascript
// 题目对象
{
  id: number,           // 题目序号
  question: string,     // 题目内容
  options: string[4],   // 选项数组 [A, B, C, D]
  answer: string,       // 正确答案 (A/B/C/D)
  type: 'single' | 'judge' // 题型
}

// Excel格式
| 正确答案 | 题目内容 | 选项A | 选项B | 选项C | 选项D |

// 错题记录
{
  questionId: number,
  userAnswer: string,
  correctAnswer: string,
  timestamp: number
}

// 应用状态
{
  mode: 'home' | 'quiz' | 'result',
  quizMode: 'sequence' | 'random' | 'review',
  questions: Question[],
  currentIndex: number,
  wrongAnswers: WrongAnswer[],
  stats: { correct: number, wrong: number }
}
```

## 5. 验收标准

- [x] 可以拖拽或点击上传Excel文件
- [x] 正确解析Excel中的题目数据
- [x] 顺序刷题模式正常运作
- [x] 随机刷题模式正常运作
- [x] 错题复习模式正常运作
- [x] 答题后正确显示对错
- [x] 错题自动记录到错题本
- [x] 进度条实时更新
- [x] 统计信息准确显示
- [x] 界面美观，动画流畅
- [x] 移动端适配良好
- [x] 左右切换按钮切换题目
- [x] 滑屏手势切换题目
- [x] 点击进度条跳转指定题目
